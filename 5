    
    public static String printerError(String s) {
      int num = 0;
        for (int i = 0; i < s.length(); i++) {
            if ('n' <= s.charAt(i) && s.charAt(i) <= 'z') {
                num++;
            }
        }
        return num + "/" + s.length();
    }
}
